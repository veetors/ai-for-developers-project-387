
Ты выступаешь в роли системного аналитика и архитектора программного обеспечения.


Контекст проекта:

Сначала нужно определить, что именно должно делать приложение, и зафиксировать контракт между клиентом и сервером.
Затем на основе этого контракта последовательно собрать интерфейс, реализовать бэкенд, покрыть основные сценарии тестами и подготовить сервис к деплою.

Мы работаем в подходе Design First: сначала фиксируем API-контракт, а затем по нему отдельно реализуем фронтенд и бэкенд.

Приложение должно собираться в Docker-образ и запускаться в контейнере

Мы разрабатываем fullstack-приложение «Запись на звонок» — это упрощенный сервис бронирования времени по мотивам Cal.com.

Cal.com — это сервис, где пользователь публикует доступные интервалы, а другой человек выбирает свободное время и бронирует встречу.
У нашего приложения тот же базовый сценарий: владелец публикует доступное время для встреч, а другой пользователь выбирает свободный слот и записывается на звонок.

В проекте нет авторизации, личных кабинетов и интеграций с внешними календарями. Функциональность намеренно ограничена, т.к. это v1.

В результате должно получиться небольшое, но законченное приложение. Пользователь видит свободные слоты по 30 минут, может выбрать время и оформить запись,
а владелец календаря — посмотреть список предстоящих встреч.

То как может выглядеть интерфейс приложения можно посмотреть в директории ./screenshots_examples/ .

Проект выполняется в подходе Design First: сначала фиксируем поведение системы и API-контракт, и только потом переходим к реализации.
Фронтенд и бэкенд реализуются раздельно и взаимодействуют через API-контракт. Контракт задает границу между частями приложения и остается единым источником правды для реализации.
Для создания контракта OpenAPI используй TypeSpec.


Описание приложения:

В проекте есть две условные роли: владелец календаря и гости. При этом в проекте нет регистрации и авторизации. Владелец календаря — один заранее заданный профиль.
Этот профиль по умолчанию используется в админской части. Гость бронирует слоты без создания аккаунта и без входа в систему.

Владелец календаря может:

- Создавать типы событий. Для каждого типа события задает id, название, описание и длительность в минутах.
- Просматривает страницу предстоящих встреч, где в одном списке показаны бронирования всех типов событий.

Гость:

- Может посмотреть страницу с видами брони, где доступно название, описание и длительность.
- Выбирает тип события, открывает календарь и выбирает свободный слот в ближайшие 14 дней.
- Создает бронирование на выбранный слот.

Правило занятости:

- На одно и то же время нельзя создать две записи, даже если это разные типы событий.

Окно записи по умолчанию:

- Доступные слоты формируются на 14 дней, начиная с текущей даты.
- Гость может записаться только на свободный слот из этого окна.


Технологический стек проекта:

Backend:
- Python 3.14
- Poetry
- Django
- Django ORM
- PostgreSQL

Frontend:
- TypeScript
- Tailwind CSS
- shadcn/ui
- Vite
- Prism

Инфраструктура:
- Docker Compose
- отдельный сервис backend
- отдельный сервис frontend
- отдельный сервис PostgreSQL

Тестирование:
- Playwright для приемочных E2E-тестов

## Доменные сущности

### Владелец календаря (Owner)
- В системе ровно один предзаданный профиль. Регистрация и авторизация отсутствуют.
- Атрибуты: id, name, timezone (фиксировано Europe/Moscow).
- Профиль создаётся сидом при миграции (id = 1) и не редактируется через API.
- Возможности:
  - Создавать, редактировать, удалять типы событий.
  - Просматривать список предстоящих бронирований (от текущего момента и позже) по всем типам событий в одном списке, упорядоченный по start_at по возрастанию.

### Тип события (EventType)
- Шаблон встречи, на которую записывается гость.
- Атрибуты: id, name, description, duration_minutes.
- В v1 значение duration_minutes фиксировано равно 30; поле присутствует в схеме и хранится в БД для совместимости с будущими версиями, но API принимает только значение 30.
- Принадлежит единственному владельцу.
- CRUD-операции доступны владельцу; гостю доступно чтение публичного каталога без авторизации.

### Слот (Slot)
- Вычисляемая сущность, в БД не хранится.
- Шаг сетки: duration_minutes выбранного типа события (30 минут в v1).
- Окно доступности: 14 календарных дат, начиная с сегодняшней даты (сегодня + 13 дней включительно).
- Рабочие часы: 06:00 – 22:00 по Europe/Moscow.
- Слот идентифицируется моментом начала start_at (datetime, Europe/Moscow).
- Слот считается доступным (свободным), если одновременно:
  - дата start_at входит в 14-дневное окно;
  - время start_at входит в рабочий диапазон 06:00–22:00 MSK;
  - start_at >= текущего момента;
  - ни одно существующее бронирование не пересекается с интервалом [start_at, start_at + 30 мин).

### Бронирование (Booking)
- Атрибуты: id, event_type_id (FK), guest_name, guest_email, start_at, end_at, created_at.
- end_at = start_at + duration_minutes вычисляется на сервере и сохраняется.
- Правило занятости: недопустимо существование двух бронирований с одинаковым start_at, независимо от типа события.
- Серверные проверки при создании:
  - слот свободен по определению выше;
  - guest_name — непустая строка длиной до 200 символов;
  - guest_email — формат email.
- В v1 бронирование не редактируется и не отменяется.

### Публичный сценарий гостя
1. Открывает главную — лендинг с кнопкой «Записаться».
2. Переходит в каталог типов событий (name, description, duration_minutes).
3. Выбирает тип — страница выбора времени: календарь на 14 дней (вне окна — неактивны) и список слотов дня с пометками «Свободно» / «Занято».
4. Выбирает дату и слот — формируется черновик (start_at).
5. Подтверждает — форма контактных данных (name, email).
6. Отправляет — сервер создаёт бронирование и возвращает подтверждение (id, start_at, тип события).
7. На всех шагах регистрация и вход не требуются.

### Хранение и отображение времени
- Все моменты времени в БД и API хранятся в UTC (ISO 8601 с timezone).
- В интерфейсе админской части и публичной части время отображается в Europe/Moscow.

Важное ограничение:

На этом этапе не нужно писать код, создавать файлы, устанавливать зависимости или вносить изменения в проект. Нужно только провести анализ задачи и подготовить материалы для следующего архитектурного этапа.


Важно:

- Не писать код.
- Не предлагать избыточную микросервисную архитектуру.
- Не усложнять систему авторизацией, если это не требуется для первой версии.
- Не добавлять функциональность, которая не указана в задаче.
- Не переходить к реализации.
- Все выводы должны быть пригодны для последующего архитектурного проектирования.
